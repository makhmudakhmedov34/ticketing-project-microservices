spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri:
          default-label:  master
          username:
          password:
    fail-fast: true

server:
  port: 8888